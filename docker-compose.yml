version: "3.8"

services:
  app:
    image: superklok/trevmorinpointca:1.7.7
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production