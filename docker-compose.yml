version: "2.4"

services:
  app:
    image: trevmorinpointca:1.1.0
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development